[project]
name = "ib-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ib-async>=2.1.0",
    "loguru>=0.7.3",
    "python-dotenv>=1.2.2",
    "pyzmq>=27.1.0",
    "tornado>=6.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
